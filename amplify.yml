version: 1
backend:
  phases:
    build:
      commands:
        - npm ci
        - npx ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist # Change to '.next' or 'build' depending on your framework
    files:
      - '**/*'
  cache:
    paths:
      - .npm/**/*
      - node_modules/**/*
